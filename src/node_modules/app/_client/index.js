import "es5-shim"
import _ from "lodash"
import React from "react"
import Block from "app/block/Block"
import "app/_client/widgets"
import sampleBlocks from "app/_client/sampleBlocks"

const block = _.find(sampleBlocks, b => b.id === 1)

React.render(<Block block={block}/>, document.getElementById("appContent"))
