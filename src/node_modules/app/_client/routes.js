const routes = {
  getComponents: done => done(null, require("app/_client/views/App")),
  childRoutes: [
    {
      path: "block",
      ...require("app/block/_client/routes")
    }
  ]
}

export default routes
