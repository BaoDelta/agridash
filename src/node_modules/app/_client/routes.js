const routes = {
  getComponents: done => done(null, require("app/_client/views/App")),
  childRoutes: [
    {
      path: "report",
      ...require("app/report/_client/routes")
    }
  ]
}

export default routes
