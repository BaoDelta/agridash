import React from "react"
import cx from "classnames"
import theme from "app/_client/views/theme.css"
import Link from "app/_client/views/Link"

const {Component, PropTypes} = React

class App extends Component {

  static propTypes = {
    children: PropTypes.node.isRequired
  }

  render() {
    const {children} = this.props
    return <div>
      <nav className={cx("navbar navbar-static-top navbar-dark", theme.primaryBackgroundColor)}>
        <button className="navbar-toggler hidden-sm-up" type="button"
            data-toggle="collapse" data-target="#menu">
          <i className="fa fa-bars"></i>
        </button>
        <div className="collapse navbar-toggleable-xs container" id="menu">
          <ul className="nav navbar-nav">
            <li className="nav-item">
              <Link className="nav-item" to="/">Home</Link>
            </li>
            <li className="nav-item">
              <Link className="nav-item" to="/block">List</Link>
            </li>
          </ul>
        </div>
      </nav>
      <div className="container m-y">
        {children}
      </div>
    </div>
  }

}

export default App
