import {find} from "app/_shared/utils"

const series = [
  {
    id: "a",
    label: "A"
  },
  {
    id: "b",
    label: "B"
  },
  {
    id: "c",
    label: "C"
  }
]

const fields = [
  {
    id: "a"
  },
  {
    id: "b"
  },
  {
    id: "c"
  }
]

const blocks = [
  {
    id: 1,
    title: "Bar: change over time",
    loader: {
      type: "mock",
      timeRange: [2004, 2014],
      fields
    },
    widget: {
      type: "bar",
      category: "time",
      xAxis: {
        label: "Year"
      },
      yAxis: {
        label: "Sales"
      },
      series
    }
  }
]

export function findAll() {
  return Promise.resolve(blocks)
}

export function findById(id) {
  return Promise.resolve(find(blocks, block => block.id === id))
}
