import {findAll, findById} from "app/api/mock/report"

export function getAllReports(request, reply) {
  findAll().then(reply)
}

export function getReport(request, reply) {
  findById(+request.params.id).then(reply)
}
