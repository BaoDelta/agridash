import {register} from "app/block/_client/widget"

register({
  table: require("app/block/_client/views/WidgetTable"),
  chartLine: require("app/block/_client/views/WidgetChartLine"),
  chartBar: require("app/block/_client/views/WidgetChartBar")
})

const routes = {
  getComponents: done => done(null, require("app/block/_client/views/BlockList")),
  childRoutes: [
    {
      path: ":id",
      getComponents: done => done(null, require("app/block/_client/views/Block"))
    }
  ]
}

export default routes
