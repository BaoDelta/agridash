import sql from "app/core/utils/sql"
import cache from "app/core/utils/cache"

export function findBySource(source) {
  return cache(`geo/${source}`, cache.EX_LONG, () => sql.select("code", "name").from("geo").where("source", source))
}
