import sql from "app/core/utils/sql"
import {fromCache, TTL_LONG} from "app/core/utils/cache"

export function findBySource(source) {
  return fromCache(`geo/${source}`, TTL_LONG, () => sql.select("id", "code", "name")
    .from("geo").where("source", source).orderBy("name"))
}
