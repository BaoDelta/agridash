import Chance from "chance"
import farmhash from "farmhash"
import {isString} from "app/core/utils"

function random(seed) {
  if (isString(seed)) {
    seed = farmhash.hash32(seed)
  }
  return new Chance(seed)
}

export default random
