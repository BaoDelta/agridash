import {renderArea} from "app/tile/handlers"

export function register(server, config, next) {
  server.route([
    {
      method: "GET",
      path: "/area/{z}/{x}/{y}.png",
      handler: renderArea
    }
  ])
  next()
}

register.attributes = {
  name: "agridash-tile"
}
