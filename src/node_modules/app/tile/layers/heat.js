export createResults from "app/tile/layers/createResults"

export function createQuery(layer, options) {
  return {
    bufferSize: 64,
    geomColumn: "pos",
    style: `<Style name="${options.layerName}" image-filters="colorize-alpha(blue, cyan, green, yellow, red)">
      <Rule>
        <Filter>[result] != ''</Filter>
        <MarkersSymbolizer allow-overlap="true" file="images/heat.png"/>
      </Rule>
    </Style>`
  }
}
