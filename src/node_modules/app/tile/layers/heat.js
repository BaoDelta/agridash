function heat(options) {
  return {
    bufferSize: 64,
    geomColumn: "pos",
    style: `<Style name="${options.styleName}" image-filters="colorize-alpha(blue, cyan, green, yellow, red)">
      <Rule>
        <MarkersSymbolizer allow-overlap="true" file="images/heat.png"/>
      </Rule>
    </Style>`
  }
}

export default heat
