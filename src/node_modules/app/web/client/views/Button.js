import React from "react"
import cx from "classnames"

function Button(props) {
  const {children, className, ...others} = props
  return <button type="button" className={cx("btn", className)} {...others}>
    {children}
  </button>
}

export default Button
