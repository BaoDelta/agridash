import React from "react"
import cx from "classnames"

function Icon(props) {
  const {icon, size, spin, className} = props
  return <i className={cx("fa", "fa-" + icon, size && "fa-" + size + "x", spin && "fa-spin", className)}></i>
}

export default Icon
