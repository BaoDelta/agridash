import React from "react"
import cx from "classnames"
import {Link as RouteLink} from "react-router"

function Link(props) {
  const {className, ...others} = props
  return <RouteLink className={cx(className, "nav-link")} {...others}/>
}

export default Link
