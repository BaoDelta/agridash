function createSelectOptions(options) {
  return {
    type: "select",
    noneLabel: "-- Select --",
    removeDefaultNone: false,
    disableSort: true,
    ...options
  }
}

export default createSelectOptions
