import createMultiSelectOptions from "app/web/forms/createMultiSelectOptions"

export function getSchema() {
  return {
    type: "array",
    items: {
      type: "string"
    }
  }
}

export function getOptions() {
  return createMultiSelectOptions({
    label: "Locations"
  })
}
