import {createMultiSelectOptions} from "app/web/forms/common"

export function getSchema() {
  return {
    type: "array",
    items: {
      type: "string"
    }
  }
}

export function getOptions() {
  return createMultiSelectOptions({
    label: "Locations"
  })
}
