import {handleActions} from "redux-actions"

import {FORM_REFRESH_DATA_SOURCE} from "app/web/actions/form"

const formReducer = handleActions({
  [FORM_REFRESH_DATA_SOURCE]: (state, action) => {
    return {
      ...state,
      dataSource: {
        ...state.dataSource,
        ...action.payload
      }
    }
  }
}, {
  dataSource: {}
})

export default formReducer
