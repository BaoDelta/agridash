import {handleActions} from "redux-actions"

import {GEO_REQUEST_GEOS, GEO_RECEIVE_GEOS} from "app/web/actions/geo"

const geoReducer = handleActions({
  [GEO_REQUEST_GEOS]: (state, action) => {
    return {
      ...state,
      [action.payload.source]: {
        promise: action.payload.promise
      }
    }
  },
  [GEO_RECEIVE_GEOS]: (state, action) => {
    return {
      ...state,
      [action.payload.source]: {
        promise: null,
        geos: action.payload.geos
      }
    }
  }
}, {})

export default geoReducer
