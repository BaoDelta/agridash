import {handleActions} from "redux-actions"

import {UPDATE_INFO} from "app/web/actions/info"

const infoReducer = handleActions({
  [UPDATE_INFO]: (state, action) => ({
    ...state,
    ...action.payload
  })
}, {})

export default infoReducer
