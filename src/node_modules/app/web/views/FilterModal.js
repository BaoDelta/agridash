import React from "react"
import {Modal, Body, Footer} from "app/web/views/Modal"
import Button from "app/web/views/Button"

const ID = "filterModal"

export const TARGET_ID = `#${ID}`

function FilterModal() {
  return <Modal id={ID} title="Filter">
    <Body>
      <p>Filter options</p>
    </Body>
    <Footer>
      <Button className="btn-primary">Apply</Button>
    </Footer>
  </Modal>
}

export default FilterModal
