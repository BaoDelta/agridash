import React from "react"
import cx from "classnames"

function Icon({icon, size, fw, spin, className}) {
  return <i className={cx(`fa fa-${icon}`, size && `fa-${size}x`, fw && "fa-fw", spin && "fa-spin", className)}></i>
}

export default Icon
