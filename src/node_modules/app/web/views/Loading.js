import React from "react"
import Icon from "app/web/views/Icon"

function Loading(props) {
  return <div className="text-center" style={{margin: "2rem"}}>
    <Icon icon="sun-o" size={2} spin {...props}/>
  </div>
}

export default Loading
