import React from "react"
import Link from "app/web/views/Link"
import Button from "app/web/views/Button"
import {noPadding} from "app/web/views/App.css"

function NotFound({history}) {
  return <div className="alert alert-warning">
    <strong>Page Not Found!</strong>
    {" "}
    You can return to the
    {" "}
    <Button className={`btn-link ${noPadding}`} onClick={() => history.goBack()}>previous page</Button>
    {" "}
    or visit <Link to="/">Home</Link>.
  </div>
}

export default NotFound
