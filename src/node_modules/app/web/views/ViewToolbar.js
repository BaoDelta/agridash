import React from "react"
import cx from "classnames"
import Icon from "app/web/views/Icon"
import Button from "app/web/views/Button"
import {TARGET_ID as FILTER_ID} from "app/web/views/FilterModal"

function ViewToolbar({className}) {
  return <div className={cx("btn-group btn-group-sm", className)}>
    <Button className="btn-primary" data-toggle="modal" data-target={FILTER_ID}><Icon icon="edit"/></Button>
    <Button className="btn-primary"><Icon icon="share-alt"/></Button>
  </div>
}

export default ViewToolbar
